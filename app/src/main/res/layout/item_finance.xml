<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="@dimen/stdMargin">
    <RelativeLayout
        android:layout_width="@dimen/width_item_finance"
        android:layout_height="@dimen/height_item_finance">

        <TextView
            android:id="@+id/viewAttrName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="2"
            tools:text="@tools:sample/cities"
            android:background="@android:color/black"
            android:textColor="@android:color/white"
            android:textAlignment="center"
            android:alpha="0.5"
            />

        <TextView
            android:id="@+id/viewValue"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            tools:text="@tools:sample/first_names"
            android:textSize="@dimen/bigText"
            android:textAlignment="center"
            android:maxLines="1"
            android:layout_below="@id/viewAttrName"
            android:layout_alignStart="@id/viewAttrName"
            android:layout_alignEnd="@id/viewAttrName"/>
    </RelativeLayout>

</androidx.cardview.widget.CardView>